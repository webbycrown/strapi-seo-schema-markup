import { PLUGIN_ID } from './pluginId';

const PERMISSIONS = {
  readCollectionTypes: [],
  manageTemplate: [],
};

const PLUGIN_API = `/${PLUGIN_ID}`;

export { PLUGIN_ID, PERMISSIONS, PLUGIN_API };
